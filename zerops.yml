# Service hostname to which the application will be deployed.
app:
  build:
    base: [nodejs@18]
    build:
      - npm i
      - git clone https://github.com/twentyhq/twenty.git
    cache: [node_modules]
    deploy: [index.js, index.html, favicon.ico, node_modules, package.json]
  run:
    start: npm start